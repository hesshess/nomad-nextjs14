import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Not Found',
};

function NotFound(cl) {
  return <div>Not Found</div>;
}

export default NotFound;
